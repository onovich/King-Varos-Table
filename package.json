{
  "name": "varos-table",
  "private": true,
  "version": "0.2.0",
  "description": "A no-guess 3x3 neighborhood logic puzzle prototype with MiniZinc-verified levels.",
  "scripts": {
    "start": "python -m http.server 4173 --directory web",
    "test": "python -m unittest discover -s tests",
    "generate": "python tools/generate_level.py --output web/data/demo-level.json"
  }
}
