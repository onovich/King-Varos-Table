# 宴厅第六稿验证记录

- 改动：火焰根部固定，顶端弯曲、宽高变化；五支烛火节奏错开，点光源亮度同步，保持两盏缓存阴影。
- 替换七张宴席椅与御座：共享本地 Blender GLB，弧形木框、收分腿、软包、金边。水果、餐盘、桌面资产未改。
- 资源：GLB 590800 字节，离线 proof JPEG 36382 字节；无外部下载和运行时 CDN。
- 生成：Blender 无窗口，BelowNormal，双线程、两颗逻辑 CPU，CPU 渲染。
- 测试：86 项 unittest 全部通过；GLTFLoader 实际解析验证尺寸、三个网格和共享几何；火焰变化幅度、错相、固定根部、光源同步、减少动态模式验证通过。git diff --check 通过。
- 限制：CUA 初始化报系统找不到指定路径；未完成本轮网页实际截图、交互回归和 FPS 测量。离线图片不代表实时画面。
- 未修改正式游戏业务或存储接口；未提交、推送或部署。
